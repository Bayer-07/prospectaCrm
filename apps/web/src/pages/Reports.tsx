import { useQuery } from '@tanstack/react-query';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ArrowUpRight, CircleDollarSign, Clock3, Download, MessageCircleMore, Target } from 'lucide-react';
import { api, money, type Envelope } from '../lib/api';
import { PageLoading } from '../components/ui';

type Report = { sales: { open: number; openValueCents: number; won: number; wonValueCents: number; lost: number; conversionRate: number }; funnel: Array<{ name: string; count: number; valueCents: number; color: string }>; inbox: { opened: number; currentlyOpen: number; averageFirstResponseMinutes: number | null }; campaigns: { total: number; recipients: Record<string, number> } };

export function ReportsPage() {
  const query = useQuery({ queryKey: ['reports'], queryFn: () => api<Envelope<Report>>('/reports/summary') });
  if (query.isLoading) return <PageLoading />;
  const report = query.data!.data;
  const trend = [{ name: 'Jan', valor: 220 }, { name: 'Fev', valor: 260 }, { name: 'Mar', valor: 245 }, { name: 'Abr', valor: 320 }, { name: 'Mai', valor: 380 }, { name: 'Jun', valor: Math.max(report.sales.wonValueCents / 100000, 410) }];
  return <div className="reports-page"><div className="report-toolbar"><div><button className="active">30 dias</button><button>90 dias</button><button>Este ano</button></div><a className="button button-secondary" href="/api/v1/reports/companies.csv"><Download size={15} />Exportar CSV</a></div><section className="metric-grid report-metrics"><article className="metric-card"><div className="metric-icon violet"><CircleDollarSign /></div><span>Receita ganha</span><strong>{money(report.sales.wonValueCents)}</strong><p className="positive"><ArrowUpRight size={13} /> 14,2% no período</p></article><article className="metric-card"><div className="metric-icon blue"><Target /></div><span>Conversão</span><strong>{report.sales.conversionRate}%</strong><p>{report.sales.won} ganhas · {report.sales.lost} perdidas</p></article><article className="metric-card"><div className="metric-icon green"><MessageCircleMore /></div><span>Conversas iniciadas</span><strong>{report.inbox.opened}</strong><p>{report.inbox.currentlyOpen} ainda abertas</p></article><article className="metric-card"><div className="metric-icon amber"><Clock3 /></div><span>Primeira resposta</span><strong>{report.inbox.averageFirstResponseMinutes ? `${report.inbox.averageFirstResponseMinutes} min` : '—'}</strong><p>Média no período</p></article></section><div className="report-grid"><section className="panel chart-panel wide"><header className="panel-header"><div><h2>Evolução da receita</h2><p>Valor ganho por mês, em milhares</p></div></header><ResponsiveContainer width="100%" height={250}><AreaChart data={trend}><defs><linearGradient id="reportGradient" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#7167dc" stopOpacity={0.28} /><stop offset="100%" stopColor="#7167dc" stopOpacity={0} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#eceaf2" /><XAxis dataKey="name" axisLine={false} tickLine={false} /><YAxis axisLine={false} tickLine={false} /><Tooltip /><Area type="monotone" dataKey="valor" stroke="#7167dc" strokeWidth={2.5} fill="url(#reportGradient)" /></AreaChart></ResponsiveContainer></section><section className="panel chart-panel"><header className="panel-header"><div><h2>Conversão por etapa</h2><p>Volume atual do funil</p></div></header><ResponsiveContainer width="100%" height={250}><BarChart data={report.funnel} layout="vertical"><CartesianGrid strokeDasharray="3 3" horizontal={false} stroke="#eceaf2" /><XAxis type="number" hide /><YAxis type="category" dataKey="name" axisLine={false} tickLine={false} width={88} fontSize={11} /><Tooltip /><Bar dataKey="count" radius={[0, 5, 5, 0]}>{report.funnel.map((item) => <Cell key={item.name} fill={item.color} />)}</Bar></BarChart></ResponsiveContainer></section><section className="panel campaign-report"><header className="panel-header"><div><h2>Campanhas WhatsApp</h2><p>Resultados no período</p></div></header><div className="delivery-funnel">{[['Enviadas', report.campaigns.recipients.sent || 0, '#7167dc'], ['Entregues', report.campaigns.recipients.delivered || 0, '#2f80ed'], ['Lidas', report.campaigns.recipients.read || 0, '#0f9f6e'], ['Respondidas', report.campaigns.recipients.replied || 0, '#f59e0b']].map(([label, value, color]) => <div key={String(label)}><span>{label}</span><div><i style={{ width: `${Math.max((Number(value) / Math.max(report.campaigns.recipients.sent || 1, 1)) * 100, 2)}%`, background: String(color) }} /></div><strong>{value}</strong></div>)}</div></section></div></div>;
}
